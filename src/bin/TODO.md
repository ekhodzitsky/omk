# TODO — Binary Crates

## Active
- [ ] `validate-contracts.rs` should also validate `TODO.md` and `AGENTS.md` presence per module.
- [ ] Add CI gate that fails when `src/bin/` binaries exceed 400 lines.

## Later
- Consider moving `validate-contracts.rs` to a standalone crate if it grows beyond 400 lines.
