// Ralph persistent loop — prd.json + verify/fix
use anyhow::Result;

pub async fn run_ralph(task: &str, dir: &std::path::Path, max_iterations: usize) -> Result<()> {
    // TODO: implement persistence loop
    Ok(())
}
